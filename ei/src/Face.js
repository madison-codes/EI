import React, { Component } from 'react';

export default (props) => {
  // here you can write logic to render differt faces here based on the props that are passed in
  return(
    <section className='face-container'>
      <h1 className="App-main-face">😊</h1>
    </section>
  )
}
